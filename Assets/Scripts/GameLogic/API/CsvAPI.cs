using System;
using System.Collections.Generic;
using Spate;

public class CsvAPI
{
    #region Card
    //public static List<RolecardCsvData> GetRoleCardList(eJobType type)
    //{
    //    List<RolecardCsvData> list = new List<RolecardCsvData>();
    //    foreach (RolecardCSV csv in DataManager.GetTable<RolecardCsvData>().Values)
    //    {
    //        if (csv.job == (int)type)
    //        {
    //            list.Add(csv);
    //        }
    //    }
    //    return list;
    //}

    //public static int ComputeEvaluateScore(EquipData data, out EquipEvaluateCsvData evaluateCsvData)
    //{
    //    int score = (int)(data.atk * 1.5f + data.hp * 2);
    //    evaluateCsvData = DataAPI.GetEquipEvaluate(score);
    //    return score;
    //}

    #endregion
}
